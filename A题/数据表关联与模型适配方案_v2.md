# MCM 2026 A题：完整模型组合与数据适配方案 (v2.0)

> **Version**: 2.0 (E1/E2/E3 升级为 MUST)  
> **Date**: 2026-01-30  
> **Status**: 数据验证完成 ✅  
> **关键更新**: E1波动建模、E2温度耦合、E3老化建模升级为必做项

---

## ⚠️ **数值免责声明 (Numerical Disclaimer)**

**本文档中所有数值示例（包括但不限于功耗值、TTE预测、阈值、系数等）均为演示性占位符 (Placeholder Examples)**。

这些数值用途：
1. **结构指导**: 展示数据流和模型架构
2. **可行性验证**: 证明数据表关联逻辑正确
3. **量纲检查**: 确保单位转换无误

**实际实施时必须**：
- ✅ 运行完整数据预处理管道 (`aggregated.csv` + `Dataset#3/#5.xlsx`)
- ✅ 用真实拟合结果替换所有占位符
- ✅ 验证派生字段（如 `Q_eff_C`, `dSOC_dt_est_per_s`）的计算准确性
- ✅ 与 `Model_Formulas_Paper_Ready.md` 保持数值一致性

---

## 目录

1. [模型组合方案 (E1-E3 → MUST)](#1-模型组合方案)
2. [数据文件完整性验证](#2-数据文件完整性验证)
3. [数据表关联设计](#3-数据表关联设计)
4. [预处理需求清单](#4-预处理需求清单)
5. [Task 1-4 数据流可行性分析](#5-task-1-4-数据流可行性分析)
6. [数据质量检查清单](#6-数据质量检查清单)
7. [实施优先级与时间线](#7-实施优先级与时间线)

---

## 1. 模型组合方案

### 1.0 🌟 核心创新点定位

**明确: 我们的创新NOT在微分方程架构，而在数据融合与场景拓展**

| 创新维度 | 具体体现 | 数据支持 |
|---------|---------|----------|
| 🌟 多源数据融合 | AndroWatts × Mendeley Cartesian Product | 36,000场景 |
| 🌟 分层建模对比 | 纯电池(Mendeley) vs 复杂系统(AndroWatts融合) | Type A vs Type B |
| 🌟 跨场景拓展性 | 1000功耗模式 × 36老化状态全交叉 | master_table |
| 🌟 模型-现实偏差分析 | 揭示系统级损耗来源 | 两类模型对比 |

### 1.1 分层建模架构 (🔴 MUST - Task 1核心)

> **⚠️ 重要说明**: 以下公式仅为**理论形式**，具体的扩展（如交互项、非线性项等）需要根据实际数据拟合结果决定。公式的最终形式由数据驱动，不是预先确定的。

| 模型类型 | ID | 模型名称 | 理论公式形式 | 数据源 | 备注 |
|---------|----|---------|---------|---------|--------------|
| **Type A: 纯电池模型** | M1-Pure | SOC基础ODE | `dSOC/dt = -I(t)/Q_eff` | Mendeley单电池 | 公式形式由Mendeley数据拟合决定 |
| **Type B: 复杂系统模型** | M1-System | SOC系统级ODE | `dSOC/dt = -P_total(t) / Q_eff` | AndroWatts×Mendeley | 具体P_total分解由数据决定 |
| **功耗分解** | M2 | 5因子功耗 | `P_total = Σ P_i` | aggregated.csv | 权重/交互项由数据拟合 |
| **TTE积分** | M3 | TTE积分 | `TTE = ∫[0,t*]dt, SOC(t*)=5%` | 派生 | 积分方法由数据特性决定 |
| **不确定性** | M4 | Bootstrap CI | `CI = [percentile_2.5, percentile_97.5]` | 派生 | - |
| **敏感性** | M5 | Sobol敏感性 | `S_i = Var(E[Y|X_i]) / Var(Y)` | 派生 | - |
| **OCV多项式** | M6 | OCV(SOC) | `V = c0 + c1·SOC + ... + cn·SOC^n` | battery_state_table | 阶数n由拟合优度决定 |
| **质量判定** | M7 | Well/Poorly | `MAPE阈值` | 派生 | 阈值由验证结果调整 |
| **建议追溯** | M8 | 建议模型 | `∂TTE/∂param` | 敏感性结果 | - |

### 1.2 升级模型 (原 COULD → 现 MUST)

| ID | 模型名称 | 数学形式 | 优先级变更 | 升级理由 | Task映射 |
|----|---------|---------|-----------|---------|----------|
| **E1** | 波动建模(OU过程) | `dX = θ(μ-X)dt + σdW` | 🟢→🔴 **MUST** | 原文明确要求"fluctuations" | Task 3 - 使用模式波动 |
| **E2** | 温度耦合 | `Q_eff(T) = Q_nom × f_temp(T)` | 🟢→🔴 **MUST** | 原文提及"temperature" | Task 1 - 温度影响 |
| **E3** | 电池老化 | `SOH = Q_actual/Q_design, f_aging(cycles)` | 🟢→🔴 **MUST** | Task 1核心(影响Q_eff) + Task 4建议 | Task 1模型 + Task 4阈值 |

**🔴 重要说明**:
- **E3老化模型**必须在Task 1中体现 (通过`f_aging`修正`Q_eff`)
- 同时在Task 4给出基于SOH阈值的用户建议
- 数据支持: battery_state_table含36种老化状态 (SOH 0.63~1.0)

### 1.3 模型-现实偏差分析框架 (🔴 MUST - Task 1论文亮点)

**⚠️ 术语澄清**:
- **"理论" = 基于现有开源数据集的模型预测** (AndroWatts/Mendeley/NASA/Oxford等)
- **"现实" = 用真实设备实测验证** (如用户自己的手机、Apple官方规格等)

**战略目标**: 开源数据集训练的模型大概率与真实设备有偏差，需要系统性分析原因。

```yaml
model_reality_gap_analysis:
  
  # ===== 术语定义 =====
  terminology:
    theoretical_model:
      definition: "基于开源数据集(AndroWatts/Mendeley等)训练和预测的模型"
      data_source: "公开数据集 - 他人设备/实验室环境"
      examples:
        - "AndroWatts: 1000个Android手机功耗测试"
        - "Mendeley: 实验室标准条件下的电池老化数据"
        - "NASA/Oxford: 受控环境电池循环数据"
    
    real_world_validation:
      definition: "用真实设备进行的实测验证"
      data_source: "用户自有设备或厂商公开规格"
      examples:
        - "用户自己的iPhone/Android手机实测"
        - "Apple官方规格 (Video Playback: 20h)"
        - "GSMArena电池续航测试结果"
  
  # ===== 偏差来源分析 =====
  gap_sources:
    source_1_device_heterogeneity:
      description: "设备异质性 - 数据集设备与验证设备不同"
      example: "AndroWatts用Google Pixel测试，验证用iPhone"
      expected_impact: "10-30%预测偏差"
      mitigation: "在论文中明确适用范围限制"
    
    source_2_environment_difference:
      description: "测试环境差异 - 实验室vs日常使用"
      example: "Mendeley 25°C恒温 vs 用户室外0-40°C"
      expected_impact: "5-15%容量偏差 (温度效应)"
      mitigation: "E2温度耦合模型进行修正"
    
    source_3_usage_pattern_mismatch:
      description: "使用模式不匹配 - 测试场景vs真实场景"
      example: "AndroWatts固定测试 vs 用户随机使用"
      expected_impact: "15-25%功耗偏差"
      mitigation: "E1 OU过程捕捉波动"
    
    source_4_battery_age_unknown:
      description: "电池老化状态未知 - 数据集新电池vs用户旧电池"
      example: "测试用新电池SOH=1.0，用户电池SOH=0.8"
      expected_impact: "20%容量偏差"
      mitigation: "E3老化模型 + 提示用户查看Battery Health"
    
    source_5_os_version_difference:
      description: "操作系统差异 - 测试OS vs 用户OS"
      example: "Android 12测试 vs Android 14/iOS 17验证"
      expected_impact: "5-10%功耗差异 (省电策略优化)"
      mitigation: "在Limitations中说明"
  
  # ===== 验证策略 =====
  validation_strategy:
    primary_validation:
      method: "与厂商公开规格对比 (Apple Specs, GSMArena)"
      advantage: "权威、可引用、覆盖面广"
      data: "apple_iphone_battery_specs.csv"
    
    secondary_validation:
      method: "与Oxford/NASA实验室数据对比 (电池层面)"
      advantage: "科学严谨、可复现"
      data: "oxford_summary.csv, NASA metadata.csv"
    
    optional_self_test:
      method: "用户自测验证 (可选)"
      procedure:
        - "记录手机型号和Battery Health"
        - "在指定场景下测试实际续航"
        - "与模型预测对比"
      note: "自测数据若使用，需上传至GitHub并注明链接"
  
  # ===== 论文写作建议 =====
  paper_recommendations:
    task_1_model_section:
      - "明确说明: 模型基于开源数据集训练"
      - "列出数据集特性: 设备类型、测试条件、时间范围"
    
    task_2_results_section:
      - "与Apple Specs/GSMArena对比展示预测准确度"
      - "分析偏差来源并量化各因素贡献"
      - "绘制: 预测TTE vs 厂商规格TTE 散点图"
    
    task_4_recommendations:
      - "提示用户: 预测适用于特定设备类型"
      - "建议用户查看自身电池健康状态调整预期"
      - "提供误差修正因子 (如: 旧电池×0.8)"
    
    limitations_section:
      - "数据集来源限制 (Android为主)"
      - "测试环境与日常使用差异"
      - "OS版本演进可能改变功耗特性"
```

### 1.3.1 模型-现实偏差分析伪代码 (实现草案)

```python
import pandas as pd


def build_gap_table(theoretical_tte: pd.DataFrame,
                     real_tte: pd.DataFrame,
                     meta: pd.DataFrame) -> pd.DataFrame:
    """构建模型-现实偏差分析表

    Parameters
    ----------
    theoretical_tte : DataFrame
        列: [device, scenario, tte_pred]
    real_tte : DataFrame
        列: [device, scenario, tte_obs]
    meta : DataFrame
        列: [device, os_version, ambient_temp, battery_age, test_env]

    Returns
    -------
    gap_table : DataFrame
        每行对应 (device, scenario)，包含绝对/相对误差以及主导偏差来源标签
    """
    # 1) 按 device+scenario 对齐理论值和真实值
    df = (theoretical_tte
          .merge(real_tte, on=["device", "scenario"], how="inner")
          .merge(meta, on="device", how="left"))

    # 2) 计算误差
    df["abs_err_h"] = (df["tte_pred"] - df["tte_obs"]).abs()
    df["rel_err_pct"] = df["abs_err_h"] / df["tte_obs"].clip(lower=1e-6) * 100

    # 3) 构造规则: 将误差归因到 5 类 gap_sources
    def classify_gap(row):
        tags = []
        # device_heterogeneity: 设备族/SoC 与训练集不同
        if row.get("train_device_family") != row.get("real_device_family"):
            tags.append("device_heterogeneity")
        # environment_difference: 实验室 25°C vs 用户极端温度
        if row["ambient_temp"] < 10 or row["ambient_temp"] > 35:
            tags.append("environment_difference")
        # usage_pattern_mismatch: 真实使用模式不在训练分布内
        if row.get("scenario") in ["gaming", "navigation"] and not row.get("is_in_training_scenarios", True):
            tags.append("usage_pattern_mismatch")
        # battery_age_unknown: 电池健康度明显低于训练数据
        if row.get("battery_age", 0) > 2 or row.get("battery_health", 100) < 80:
            tags.append("battery_age_unknown")
        # os_version_difference: OS 版本跨度较大
        if row.get("os_version_gap_major", False):
            tags.append("os_version_difference")
        return ",".join(tags) if tags else "unknown"

    df["gap_tags"] = df.apply(classify_gap, axis=1)

    # 4) 输出按误差从大到小排序的分析表
    cols = [
        "device", "scenario", "tte_pred", "tte_obs",
        "abs_err_h", "rel_err_pct", "gap_tags",
        "os_version", "ambient_temp", "battery_age", "test_env",
    ]
    return df[cols].sort_values("abs_err_h", ascending=False)
```

---

## 2. 数据文件完整性验证

### 2.1 核心数据表 (逐行验证结果)

| 文件名 | 期望行数 | 实际行数 | 列数 | 验证状态 | 数据来源/追溯 |
|-------|---------|---------|------|---------|----------------|
| `MCM2026A题锂电池数据表：master_modeling_table.csv` | 36,000 | **36,001** (含表头) | 93 | ✅ **完整** | 由 AndroWatts `aggregated.csv` 功耗特征表 × Mendeley 电池状态表（36 种 SOH 档位）笛卡尔积生成 (见 3.1) |
| `MCM2026_battery_state_table.csv` | 36 | **37** (含表头) | 15 | ✅ **完整** | 由 Mendeley `Dataset#3.xlsx` / `Dataset#5.xlsx` 原始循环数据预处理拟合 OCV 多项式与 SOH 档位得到 (见 4.2) |
| `锂电池数据集：变量字典.csv` | ~94 | **95** (含表头) | 5 | ✅ **完整** | 手工整理，汇总 battery_data 目录下各数据源字段含义，作为全局字段追溯字典 |
| `open_data/.../aggregated.csv` | 1,000 | **1,001** (含表头) | 67 | ✅ **完整** | 原始开源 AndroWatts 数据集 (`open_data/material/.../aggregated.csv`，见附录 B 数据源引用) |

### 2.2 原始数据源验证

| 数据源 | 文件 | 验证状态 | 行数/条目 | 关键字段 |
|-------|------|---------|----------|----------|
| **AndroWatts (Zenodo)** | `aggregated.csv` | ✅ | 1000测试 | ID, *_ENERGY_UW, BATTERY__PERCENT |
| **Mendeley (电池老化)** | `Dataset#3.xlsx` | ✅ | 3 cells × 多样本 | Cell01/02/03 sheets |
| **Mendeley** | `Dataset#5.xlsx` | ✅ | 3 cells × 多样本 | Cell01/02/03 sheets |
| **Apple Specs (Multi-Scenario)** | `apple_validation_comparison.csv` | ✅ | 38机型 × 3场景 = 114数据点 (2014-2024) | Device, Scenario, Battery_Capacity_mAh, Apple_Official_TTE_h, Watt_Hour |
| **Oxford** | `oxford_summary.csv` | ✅ | 8 cells | Capacity_Fade_Pct, Total_Cycles |
| **NASA** | `metadata.csv` | ✅ | 7567记录 | Capacity, ambient_temperature |

### 2.3 辅助数据验证

| 文件路径 | 用途 | 行数 | 状态 |
|---------|------|------|------|
| `oxford_cell1_cycles.csv` | E3老化验证 | 158 | ✅ |
| `oxford_cell2_cycles.csv` | E3老化验证 | 147 | ✅ |
| `oxford_cell3_cycles.csv` | E3老化验证 | 153 | ✅ |
| ...(oxford_cell4-8) | E3老化验证 | ~100-160 | ✅ |

---

## 3. 数据表关联设计

### 3.1 核心关联架构 (基于用户说明文档)

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│                           数据表关联架构 (核心)                                    │
├─────────────────────────────────────────────────────────────────────────────────┤
│                                                                                  │
│   ┌───────────────────────┐         ┌───────────────────────┐                   │
│   │   AndroWatts 数据     │         │   Mendeley 电池数据    │                   │
│   │  (aggregated.csv)     │         │ (Dataset#3/#5.xlsx)    │                   │
│   │   1000 条功耗测试     │         │   6 cells × 多样本     │                   │
│   └───────────┬───────────┘         └───────────┬───────────┘                   │
│               │                                 │                               │
│               │    预处理                        │    预处理                      │
│               ↓                                 ↓                               │
│   ┌───────────────────────┐         ┌───────────────────────┐                   │
│   │   功耗特征表           │         │   电池状态表            │                   │
│   │   phone_test_id       │         │  battery_state_id     │                   │
│   │   P_total_uW          │         │  Q_full_Ah, SOH       │                   │
│   │   soc0, temp_c        │         │  ocv_c0 ~ ocv_c5      │                   │
│   │   I_obs_A             │         │  (36 种老化状态)       │                   │
│   │   (1000 行)           │         │                        │                   │
│   └───────────┬───────────┘         └───────────┬───────────┘                   │
│               │                                 │                               │
│               └─────────────┬───────────────────┘                               │
│                             │                                                   │
│                    笛卡尔积 (Cartesian Product)                                  │
│                   1000 × 36 = 36,000 行                                          │
│                             │                                                   │
│                             ↓                                                   │
│   ┌─────────────────────────────────────────────────────────────────────────┐   │
│   │                     主表 (Master Modeling Table)                         │   │
│   │         PRIMARY KEY: (phone_test_id, battery_state_id)                  │   │
│   │                       36,000 行 × 93 列                                  │   │
│   │                                                                          │   │
│   │   字段组成:                                                               │   │
│   │   ├── AndroWatts 原始字段 (69列): *_ENERGY_UW, *_FREQ_KHz, etc.          │   │
│   │   ├── 电池状态字段 (15列): Q_full_Ah, SOH, ocv_c0~c5, etc.              │   │
│   │   └── 派生字段: dSOC_dt_est_per_s, t_empty_h_est, etc.                  │   │
│   └─────────────────────────────────────────────────────────────────────────┘   │
│                                                                                  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 关键字段关联映射

| 主表字段 | 来源表 | 原始字段 | 转换公式 |
|---------|-------|---------|---------|
| `phone_test_id` | aggregated.csv | `ID` | 直接重命名 |
| `battery_state_id` | Mendeley派生 | Dataset+Cell+Label | 组合生成 |
| `soc0` | aggregated.csv | `BATTERY__PERCENT` | `/100` |
| `temp_c` | aggregated.csv | `AVG_SOC_TEMP` | `/1000` (m°C→°C) |
| `I_obs_A` | aggregated.csv | `BATTERY_DISCHARGE_RATE_UAS` | `×1e-6` |
| `P_total_uW` | aggregated.csv | `sum(*_ENERGY_UW)` | 合计 |
| `Q_full_Ah` | Mendeley | 最高电压列容量 | 直接提取 |
| `SOH` | Mendeley派生 | `Q_full/Q_full_new` | 计算 |
| `Q_eff_C` | 派生 | `Q_full_Ah × 3600` | 转换 |
| `ocv_c0~c5` | Mendeley派生 | 5次多项式拟合 | 最小二乘 |

### 3.3 辅助表关联 (用于验证和扩展)

```
┌───────────────────────────────────────────────────────────────────────────────┐
│                        辅助数据表关联 (验证 & 扩展)                             │
├───────────────────────────────────────────────────────────────────────────────┤
│                                                                                │
│   ┌─────────────────┐    验证 TTE     ┌─────────────────────┐                 │
│   │  Apple Specs    │───────────────→│  Task 2: TTE预测    │                 │
│   │  (13 机型)       │  参考基准       │  "observed behavior" │                │
│   └─────────────────┘                 └─────────────────────┘                 │
│                                                                                │
│   ┌─────────────────┐    验证 E3      ┌─────────────────────┐                 │
│   │  Oxford Data    │───────────────→│  E3: 老化模型验证    │                 │
│   │  (8 cells)      │  20-38% 衰减    │  Capacity_Fade_Pct  │                 │
│   └─────────────────┘                 └─────────────────────┘                 │
│                                                                                │
│   ┌─────────────────┐    验证 E2      ┌─────────────────────┐                 │
│   │  NASA Data      │───────────────→│  E2: 温度影响验证    │                 │
│   │  (7567 记录)    │  ambient_temp   │  f_temp(T) 校准     │                 │
│   └─────────────────┘                 └─────────────────────┘                 │
│                                                                                │
└───────────────────────────────────────────────────────────────────────────────┘
```

---

## 4. 预处理需求清单

### 4.1 AndroWatts 数据预处理 (aggregated.csv)

| 步骤 | 原始字段 | 处理方式 | 目标字段 | 优先级 |
|-----|---------|---------|---------|--------|
| 1 | `ID` | 重命名 | `phone_test_id` | 🔴 MUST |
| 2 | `AVG_SOC_TEMP` | `÷1000` (m°C→°C) | `temp_c` | 🔴 MUST |
| 3 | `BATTERY__PERCENT` | `÷100` | `soc0` | 🔴 MUST |
| 4 | `BATTERY_DISCHARGE_RATE_UAS` | `×1e-6` | `I_obs_A` | 🔴 MUST |
| 5 | `*_ENERGY_UW` (25列) | `sum()` | `P_total_uW` | 🔴 MUST |
| 6 | `*_ENERGY_AVG_UWS` (25列) | `sum()` | `E_total_uWh_est` | 🟡 SHOULD |
| 7 | `CPU_MID_FREQ_KHz` | 处理"err"值 | 数值/NaN | 🟡 SHOULD |

### 4.2 Mendeley 电池数据预处理 (Dataset#3/#5.xlsx)

| 步骤 | 处理内容 | 方法 | 优先级 |
|-----|---------|------|--------|
| 1 | 读取 Cell01/02/03 sheets | pandas.read_excel | 🔴 MUST |
| 2 | 识别电压列 (形如"x V") | 正则匹配 | 🔴 MUST |
| 3 | 提取 Q_full_Ah | 取最高电压列容量 | 🔴 MUST |
| 4 | 计算 SOH | `Q_full / Q_full_new` | 🔴 MUST |
| 5 | 选取6个老化档位 | new/slight/moderate/aged/old/eol | 🔴 MUST |
| 6 | 拟合 OCV(SOC) 多项式 | 5次最小二乘拟合 | 🔴 MUST |
| 7 | 输出 ocv_c0~c5 系数 | 存入battery_state_table | 🔴 MUST |

### 4.3 派生字段计算

| 字段 | 公式 | 依赖字段 | 用途 |
|-----|------|---------|------|
| `Q_eff_C` | `Q_full_Ah × 3600` | Q_full_Ah | M1 SOC ODE |
| `dSOC_dt_est_per_s` | `-I_obs_A / Q_eff_C` | I_obs_A, Q_eff_C | 初始化估算 |
| `t_empty_h_est` | `soc0 / (-dSOC/dt) / 3600` | soc0, dSOC/dt | 快速验证 |
| `duration_h_est` | `E_total / P_total` | E_total, P_total | 测试时长 |

### 4.4 数据质量处理

| 问题类型 | 影响字段 | 处理方案 | 影响行数 |
|---------|---------|---------|---------|
| "err"字符串 | `CPU_MID_FREQ_KHz` | 转换为 `NaN`，使用插值/中位数填充 | ~5-10行 |
| 重复列名 | `Memory_ENERGY_AVG_UWS.1` | 保留或合并 | 全部 |
| 极端值 | `BATTERY_DISCHARGE_TOTAL_UA < 0` | 保留(放电为负) | 正常 |
| 缺失值 | 少量单元格 | 使用列中位数填充 | <1% |

---

## 5. Task 1-4 数据流可行性分析

### 5.1 Task 1: 连续时间模型 - 数据流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Task 1 数据流                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   输入数据                        模型计算                       输出         │
│   ┌─────────────┐                                                           │
│   │ Master Table│                                                           │
│   │ (36000行)   │                                                           │
│   └──────┬──────┘                                                           │
│          │                                                                  │
│          ├──→ P_total_uW ──→ [M2] P = Σ P_i ──→ 功耗分解表                  │
│          │                                                                  │
│          ├──→ Q_eff_C, soc0 ──→ [M1] dSOC/dt = -P/Q ──→ SOC(t)曲线         │
│          │                                                                  │
│          ├──→ ocv_c0~c5 ──→ [M6] V(SOC) 多项式 ──→ 电压曲线                 │
│          │                                                                  │
│          └──→ temp_c ──→ [E2] Q_eff(T) = Q × f(T) ──→ 温度修正SOC(t)        │
│                                                                              │
│   可行性: ✅ 100%                                                            │
│   数据完整度: ✅ 所有必需字段已验证                                           │
│   预处理完成度: ✅ 主表已完成预处理                                           │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 Task 2: TTE预测 - 数据流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Task 2 数据流                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   输入数据                        模型计算                       输出         │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ SOC(t) 曲线 │──→ [M3] TTE = ∫dt until SOC=5% ──→ TTE预测值              │
│   └─────────────┘                                                           │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ 参数分布    │──→ [M4] Bootstrap (n=1000) ──→ 95% CI                     │
│   └─────────────┘                                                           │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ Apple Specs │──→ [M7] MAPE计算 ──→ Well/Poorly判定                      │
│   │ (验证基准)  │                                                           │
│   └─────────────┘                                                           │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ 5种场景定义 │──→ 场景模拟 ──→ TTE × 场景矩阵 (20个预测点)               │
│   │ (S1-S5)     │                                                           │
│   └─────────────┘                                                           │
│                                                                              │
│   可行性: ✅ 100%                                                            │
│   验证数据: ✅ Apple Specs提供13机型参考                                      │
│   场景支撑: ✅ aggregated.csv包含多种功耗模式                                 │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.3 Task 3: 敏感性分析 - 数据流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Task 3 数据流                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   输入数据                        模型计算                       输出         │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ 模型参数    │──→ [M5] Sobol指数 ──→ 参数敏感性排序                       │
│   │ (P_i, Q, T) │                                                           │
│   └─────────────┘                                                           │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ aggregated  │──→ [E1] OU过程拟合 ──→ 波动参数 (μ, σ, θ)                 │
│   │ 统计量      │      dX = θ(μ-X)dt + σdW                                  │
│   └─────────────┘                                                           │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ 假设变体    │──→ 假设测试 ──→ Δ_assumption 影响量化                     │
│   │ (线性/非线性)│                                                           │
│   └─────────────┘                                                           │
│                                                                              │
│   可行性: ✅ 100%                                                            │
│   E1波动数据: ✅ aggregated.csv含1000个功耗样本，可计算μ/σ                   │
│   假设测试: ✅ 主表包含新旧电池对比数据                                        │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.3.1 E1 波动建模 (Ornstein–Uhlenbeck 拟合步骤)

```python
import numpy as np
import pandas as pd


def fit_ou_from_series(x: np.ndarray, dt: float):
    """从单个功耗/负载时间序列拟合 OU 过程参数 θ, μ, σ.

    假设: x_{t+dt} - x_t ≈ θ(μ - x_t)dt + σ√dt · ε_t,  ε_t ~ N(0,1)
    """
    x_t = x[:-1]
    x_tp = x[1:]
    dx = x_tp - x_t

    # 线性回归: dx/dt = a + b x_t  →  b ≈ -θ,  a ≈ θ μ
    y = dx / dt
    X = np.vstack([np.ones_like(x_t), x_t]).T
    beta, *_ = np.linalg.lstsq(X, y, rcond=None)
    a, b = beta
    theta = -b
    mu = a / theta if theta != 0 else x.mean()

    # 残差方差 → σ
    y_hat = X @ beta
    resid = y - y_hat
    sigma = np.std(resid) * np.sqrt(dt)
    return {"theta": theta, "mu": mu, "sigma": sigma}


def aggregate_ou_params(aggregated: pd.DataFrame, col: str, dt: float) -> pd.DataFrame:
    """在 aggregated.csv 中按场景/测试样本拟合 OU 参数并做场景平均.

    Parameters
    ----------
    aggregated : DataFrame
        AndroWatts 预处理后的功耗统计表
    col : str
        需要拟合波动的列，如 'CPU_LOAD_NORM' 或 归一化 'P_total'
    dt : float
        采样时间间隔 (秒)
    """
    rows = []
    for test_id, df_group in aggregated.groupby("ID"):
        series = df_group[col].values.astype(float)
        if len(series) < 10:
            continue
        params = fit_ou_from_series(series, dt)
        params["phone_test_id"] = test_id
        rows.append(params)

    return pd.DataFrame(rows)
```

### 5.3.1 E1 波动建模 (Ornstein–Uhlenbeck 拟合步骤)

```python
import numpy as np
import pandas as pd


def fit_ou_from_series(x: np.ndarray, dt: float):
    """从单个功耗/负载时间序列拟合 OU 过程参数 θ, μ, σ.

    假设: x_{t+dt} - x_t ≈ θ(μ - x_t)dt + σ√dt · ε_t,  ε_t ~ N(0,1)
    """
    x_t = x[:-1]
    x_tp = x[1:]
    dx = x_tp - x_t

    # 线性回归: dx/dt = a + b x_t  →  b ≈ -θ,  a ≈ θ μ
    y = dx / dt
    X = np.vstack([np.ones_like(x_t), x_t]).T
    beta, *_ = np.linalg.lstsq(X, y, rcond=None)
    a, b = beta
    theta = -b
    mu = a / theta if theta != 0 else x.mean()

    # 残差方差 → σ
    y_hat = X @ beta
    resid = y - y_hat
    sigma = np.std(resid) * np.sqrt(dt)
    return {"theta": theta, "mu": mu, "sigma": sigma}


def aggregate_ou_params(aggregated: pd.DataFrame, col: str, dt: float) -> pd.DataFrame:
    """在 aggregated.csv 中按场景/测试样本拟合 OU 参数并做场景平均.

    Parameters
    ----------
    aggregated : DataFrame
        AndroWatts 预处理后的功耗统计表
    col : str
        需要拟合波动的列，如 'CPU_LOAD_NORM' 或 归一化 'P_total'
    dt : float
        采样时间间隔 (秒)
    """
    rows = []
    for test_id, df_group in aggregated.groupby("ID"):
        series = df_group[col].values.astype(float)
        if len(series) < 10:
            continue
        params = fit_ou_from_series(series, dt)
        params["phone_test_id"] = test_id
        rows.append(params)

    return pd.DataFrame(rows)
```

### 5.4 Task 4: 建议 - 数据流

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           Task 4 数据流                                      │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│   输入数据                        模型计算                       输出         │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ 敏感性排序  │──→ [M8] ∂TTE/∂param ──→ 用户建议 + 量化收益                │
│   └─────────────┘                                                           │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ SOH数据     │──→ [E3] f_aging(cycles) ──→ 老化阈值 & 行动建议            │
│   │ (0.63~1.0)  │                                                           │
│   └─────────────┘                                                           │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ Oxford Data │──→ [E3验证] Capacity_Fade_Pct ──→ 老化模型校准             │
│   │ (20-38%衰减)│                                                           │
│   └─────────────┘                                                           │
│                                                                              │
│   ┌─────────────┐                                                           │
│   │ 功耗分解    │──→ OS优化策略 ──→ 伪代码 + 预期收益                        │
│   └─────────────┘                                                           │
│                                                                              │
│   可行性: ✅ 100%                                                            │
│   E3老化数据: ✅ battery_state_table含SOH 0.63~1.0完整范围                   │
│   验证数据: ✅ Oxford提供8 cells真实衰减数据                                  │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.5 整体可行性评估

| Task | 数据完备性 | 模型支撑 | 可行性评分 | 风险项 |
|------|-----------|---------|-----------|--------|
| **Task 1** | ✅ 100% | M1, M2, M6, E2 | **100%** | 无 |
| **Task 2** | ✅ 100% | M3, M4, M7 | **100%** | 无 |
| **Task 3** | ✅ 100% | M5, E1 | **100%** | E1需从aggregated统计量拟合 |
| **Task 4** | ✅ 100% | M8, E3 | **100%** | E3需Oxford数据验证 |

---

## 6. 数据质量检查清单

### 6.1 主表检查 (master_modeling_table.csv)

| 检查项 | 预期值 | 实际值 | 状态 | 备注 |
|-------|-------|-------|------|------|
| 行数 | 36,000 | 36,001 (含表头) | ✅ | 1000×36=36000 |
| 列数 | 93 | 93 | ✅ | - |
| 主键唯一性 | 36,000 unique | 待验证 | ⚠️ | 运行时检查 |
| phone_test_id范围 | [0, 999] | [0, 999] | ✅ | - |
| SOH范围 | [0.6, 1.0] | [0.63, 1.0] | ✅ | - |
| Q_full_Ah范围 | >0 | [1.7, 28.1] | ✅ | Dataset#3小电池, #5大电池 |
| temp_c范围 | [0, 60] | [33.2, 46.6] | ✅ | 室温范围合理 |
| soc0范围 | [0, 1] | 主要集中在0.36-1.0 | ✅ | - |

### 6.2 功耗字段检查 (aggregated.csv来源)

| 字段 | 预期单位 | 有效范围 | 异常值处理 |
|-----|---------|---------|-----------|
| `*_ENERGY_UW` | µW | >0 | 0值正常(组件未使用) |
| `BATTERY_DISCHARGE_TOTAL_UA` | µA | <0(放电) | 负值正常 |
| `CPU_MID_FREQ_KHz` | kHz | >0 或 "err" | "err"→NaN |
| `AVG_SOC_TEMP` | m°C | [30000, 50000] | 转换为°C |

### 6.3 电池状态字段检查

| 字段 | 预期范围 | 物理意义验证 |
|-----|---------|-------------|
| `ocv_c0` | [2.5, 4.2] | OCV截距(空电池~新电池) |
| `ocv_c1~c5` | 任意实数 | 多项式系数 |
| `Q_eff_C` | >0 | 库仑量，应为Q_full_Ah×3600 |
| `SOH` | [0.6, 1.0] | 6档位: new~eol |

### 6.4 验证数据检查

| 数据源 | 检查项 | 状态 | 设备数量 |
|-------|-------|------|----------|
| **Apple Specs (Multi-Scenario)** | Apple_Official_TTE_h ∈ [8, 105] (含3场景) | ✅ | 114数据点 (iPhone 6 - iPhone 16 Pro Max) |
| **Apple Specs (Multi-Scenario)** | Scenario ∈ {S5_Video_Playback, S5_Video_Streaming, S6_Audio_Playback} | ✅ ⚠️ **仅3场景** | 38设备 × 3场景 |
| **Apple Specs (Multi-Scenario)** | Battery_Capacity_mAh ∈ [1624, 4685] | ✅ | 38设备范围 |
| **Apple Specs (Multi-Scenario)** | Design_Cycle_Life ∈ {500, 1000} | ✅ | 分水岭: iPhone 14→15 |
| **Apple Specs (Multi-Scenario)** | Watt_Hour ∈ [6.21, 18.08] | ✅ | 10年技术演进 (2014-2024) |
| **Apple Specs (Multi-Scenario) - 扩展目标** | 需补充场景: Gaming, Web Browsing, Standby | ⚠️ **待扩展** | 目标: 38设备 × 6场景 = 228数据点 |
| **Oxford** | Capacity_Fade_Pct ∈ [20, 38] | ✅ | 8 cells |
| **Oxford** | Total_Cycles ∈ [46, 78] | ✅ | 8 cells |
| **NASA** | Capacity > 0 | ✅ | 7567 records |

**🔴 重要说明 - 场景数据限制**:

当前 `apple_validation_comparison.csv` 中**所有行的场景列都显示为 S5_Video 相关**,这是因为:

1. **实际包含3个不同场景**:
   - `S5_Video_Playback` (视频播放, 离线)
   - `S5_Video_Streaming` (视频流媒体, 在线)
   - `S6_Audio_Playback` (音频播放)

2. **命名约定**:
   - `S5_Video_*` = 第5类场景的视频子类
   - `S6_Audio_*` = 第6类场景的音频类
   - 这是Apple官方技术规格的标准场景分类

3. **数据扩展需求** (锂电池数据资源指南.md Section 2.6 已补充30+资源):
   - **缺少场景**: Gaming (游戏), Web Browsing (网页浏览), Standby (待机)
   - **扩展来源**: GSMArena, PhoneArena, Tom's Guide, AnandTech, NotebookCheck
   - **目标**: 将114数据点扩展至 **228+ 数据点** (38设备 × 6场景)

4. **为什么不是错误**:
   - Apple官方只公布3种场景的续航数据 (Video / Audio / Talk Time)
   - 其他场景 (Gaming, Web) 需从第三方测试平台获取
   - 场景前缀 "S5_Video" 是合理的语义命名,不是重复错误

---

## 7. 实施优先级与时间线

### 7.1 模型实施顺序 (考虑依赖关系)

```
Phase 1 (Hour 12-24): 核心模型
├── M2 功耗分解 ──→ 从aggregated.csv提取P_i
├── M1 SOC ODE ──→ 使用M2输出 + Q_eff
└── M6 OCV多项式 ──→ 从battery_state_table读取

Phase 2 (Hour 24-36): 预测模型
├── M3 TTE积分 ──→ 使用M1输出
├── M4 Bootstrap CI ──→ 在M3上运行
└── M7 Well/Poorly ──→ 对比Apple Specs

Phase 3 (Hour 36-48): 敏感性 & 扩展
├── M5 Sobol分析 ──→ 使用M1-M4
├── E1 波动建模 ──→ 从aggregated统计量拟合
├── E2 温度耦合 ──→ 使用temp_c修正M1
└── E3 老化建模 ──→ 使用SOH数据

Phase 4 (Hour 48-60): 建议生成
├── M8 建议追溯 ──→ 使用M5敏感性排序
└── 整合检验 ──→ Oxford数据验证E3
```

### 7.2 数据加载代码模板

```python
import pandas as pd
import numpy as np

# ===== 1. 主表加载 =====
master_table = pd.read_csv(
    'battery_data/MCM2026A题锂电池数据表：master_modeling_table.csv'
)
print(f"主表: {len(master_table)} 行, {len(master_table.columns)} 列")

# ===== 2. 电池状态表加载 =====
battery_state = pd.read_csv('battery_data/MCM2026_battery_state_table.csv')
print(f"电池状态: {len(battery_state)} 种")

# ===== 3. 验证数据加载 =====
apple_specs = pd.read_csv('battery_data/apple/apple_iphone_battery_specs.csv')
oxford_summary = pd.read_csv('battery_data/kaggle/oxford/oxford_summary.csv')

# ===== 4. 数据质量检查 =====
assert len(master_table) == 36000, "主表行数异常"
assert master_table['SOH'].between(0.6, 1.01).all(), "SOH范围异常"
assert master_table['Q_full_Ah'].gt(0).all(), "Q_full_Ah必须为正"

# ===== 5. 关键字段提取 =====
# M1所需
Q_eff_C = master_table['Q_eff_C'].values
soc0 = master_table['soc0'].values

# M2所需 - 功耗分解
power_cols = [c for c in master_table.columns if c.endswith('_ENERGY_UW')]
P_components = master_table[power_cols]

# E2所需 - 温度
temp_c = master_table['temp_c'].values

# E3所需 - 老化
SOH = master_table['SOH'].values

print("✅ 数据加载完成，可开始建模")
```

---

## 附录 A: 变量字典摘要

| 变量 | 单位 | 定义 | 模型用途 |
|-----|------|------|---------|
| `phone_test_id` | - | 功耗测试编号 | 主键 |
| `battery_state_id` | - | 电池状态编号 | 主键 |
| `Q_full_Ah` | Ah | 满充容量 | M1 |
| `SOH` | 1 | 健康度 | E3 |
| `Q_eff_C` | C | 有效电荷 | M1 |
| `soc0` | 1 | 初始SOC | M1 |
| `temp_c` | °C | 温度 | E2 |
| `I_obs_A` | A | 放电电流 | M1 |
| `P_total_uW` | µW | 总功耗 | M2 |
| `ocv_c0~c5` | V | OCV系数 | M6 |

---

## 附录 B: 数据源引用

| 数据集 | URL | License | DOI |
|-------|-----|---------|-----|
| AndroWatts | https://zenodo.org/records/14314943 | CC BY 4.0 | 10.5281/zenodo.14314943 |
| Mendeley Battery | https://data.mendeley.com/datasets/v8k6bsr6tf/1 | CC BY 4.0 | 10.17632/v8k6bsr6tf.1 |
| Oxford Battery | 内部转换 | - | - |
| NASA Battery | https://ti.arc.nasa.gov/tech/dash/groups/pcoe/prognostic-data-repository/ | Public Domain | - |
| Apple Specs | 公开官方数据 | - | - |

---

**文档结束**

> **状态**: 数据验证完成，可开始建模  
> **下一步**: 运行附录代码验证数据加载，然后实现 M1+M2  
> **目标**: Hour 48 前完成 M1-M8 + E1-E3
